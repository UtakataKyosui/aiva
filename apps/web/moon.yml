language: javascript
dependsOn:
  - shared

tasks:
  dev:
    command: pnpm dev
    deps:
      - ^:build
    local: true
  build:
    command: pnpm build
    deps:
      - ^:build
    inputs:
      - src/**/*
      - public/**/*
      - package.json
      - tsconfig.json
      - rsbuild.config.ts
    outputs:
      - dist
  test:
    command: pnpm test
    deps:
      - ^:build
    inputs:
      - src/**/*
      - tests/**/*
      - package.json
      - tsconfig.json
      - rstest.config.ts
  lint:
    command: pnpm lint
    inputs:
      - src/**/*
      - tests/**/*
      - package.json
      - biome.json
  format:
    command: pnpm format
    local: true
